server:
  port: 8888
spring:
  application:
    name: microservices-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/brunomarques-br/springcloud-config-server
          default-label: main
          search-paths:
            - 'greeting-service*'